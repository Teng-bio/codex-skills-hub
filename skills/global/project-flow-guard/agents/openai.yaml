interface:
  display_name: "Project Flow Guard"
  short_description: "Guard runs as provenance and promote accepted outputs to current/indexes."
  default_prompt: "Use $project-flow-guard to create provenance runs with RUN_MANIFEST.json, promote accepted outputs to current/ and RESULTS_INDEX.md, or branch only confirmed long-lived directions."
