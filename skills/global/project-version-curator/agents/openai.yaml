interface:
  display_name: "Project Version Curator"
  short_description: "Audit, separate, and package mixed project versions safely."
  default_prompt: "Audit this project for mixed file versions, identify final/candidate/intermediate outputs, and propose a safe publication-release structure without deleting original files."
